# Pkg
alias u='brew update && brew upgrade'
alias i='brew install'
alias ui='brew uninstall'

# Application ID
function aid() {
	osascript -e "id of app \"$1\""
}

# GNU sed
export PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"
